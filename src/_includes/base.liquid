<!doctype html>
<html lang="nl">
  <head>
    <title>
      {% if title %}
        {{ title }} -
      {% endif %}
      Pinefolk SMP
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ '/global.css' | url}}">
    <link rel="icon" type="image/x-icon" href="{{ '/img/favicon.ico' | url }}">
  </head>
  <body>
    <div class="wrapper mc-inventory">
      {% include 'header.liquid' %}
      <div class="mc-inventory__body flow">
        {{ content }}
      </div>
    </div>
  </body>
</html>
