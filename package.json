{
  "scripts": {
    "css": "npx postcss src/css/*.css --dir dist",
    "clean": "rm -r -f dist && mkdir dist",
    "start": "npm run clean && concurrently 'npx @11ty/eleventy --serve' 'npm run css -- --watch'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "clean-css": "^5.3.2",
    "concurrently": "^8.2.2",
    "cssnano": "^6.0.1",
    "postcss": "^8.4.31",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0",
    "postcss-import-ext-glob": "^2.1.1",
    "postcss-js": "^4.0.1",
    "slugify": "^1.6.6",
    "tailwindcss": "^3.3.5"
  }
}
